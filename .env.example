# Base URL of the site (used for metadata, canonical links, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Contact form endpoint / email service (e.g. Formspree, Resend, SMTP relay)
CONTACT_FORM_ENDPOINT=

# Optional analytics ID (e.g. Google Analytics / Plausible)
NEXT_PUBLIC_ANALYTICS_ID=
